<div class="row menu-item-detail">
	<div class="col-md-8 col-md-offset-2">
		<div class="row">
			<div class="col-md-4">
				<p class="name"><%= @menu_item.name %></p>
				<p class="description"><%= @menu_item.description %></p>
				<p class="price"><%= @menu_item.price %> đ</p>
				<%= link_to "Order", order_menu_path(@menu_item.id), class: "btn btn-primary col-md-12" %>
			</div>
			<div class="col-md-8">
				<%= image_tag @menu_item.image_url %>
			</div>		
		</div>
	</div>
</div>